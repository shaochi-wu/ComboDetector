在面試當下，我嘗試使用 **Sliding Window（滑動視窗）** 的演算法來偵測格鬥遊戲的按鍵組合。但在與主管討論的過程中，我發現這個解法有一個致命的盲點：**當面對「長短不一且帶有後綴重疊」的指令（例如 `A B` 與 `Down A B`）時，固定長度的視窗會導致誤判或將招式覆蓋。**
面試結束後，這個問題引發了我的深思。為了解決 Sliding Window 的盲點，並實作出更貼近真實遊戲引擎中「大絕招優先」的判定機制，我重新梳理架構，使用 C++ 重新實作這個更穩健的版本，作為面試後的技術反思與學習紀錄。
